import { AiAllowanceScreen } from '@/src/components/shared/AiUnavailableScreen'

/**
 * Opened by the root layout the moment a chat or bill scan is refused because
 * this month's allowance is spent (see onAiAllowanceExceeded).
 */
export default AiAllowanceScreen
